# Copy this file to .env before running `docker compose up` — docker-compose.yml
# reads these to configure the local PostgreSQL container and the app's
# connection to it. Never commit a real .env file (already in .gitignore).

POSTGRES_DB=automarket
POSTGRES_USER=automarket
POSTGRES_PASSWORD=change-me-locally
